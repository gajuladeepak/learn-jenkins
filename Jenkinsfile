pipeline {
    agent {
        label 'Agent-1'
    }
    stages {
        stage('Build') { 
            steps {
                sh 'echo this is build'
            }
        }
        stage('Test') { 
            steps {
                sh 'echo this is build'
            }
        }
        stage('Deploy') { 
            steps {
                sh 'echo this is build'
            }
        }
    }
}